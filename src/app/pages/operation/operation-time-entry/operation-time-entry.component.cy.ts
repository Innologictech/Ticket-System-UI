import { OperationTimeEntryComponent } from './operation-time-entry.component'

describe('OperationTimeEntryComponent', () => {
  it('should mount', () => {
    cy.mount(OperationTimeEntryComponent)
  })
})